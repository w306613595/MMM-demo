
1、base.less中语法错误     27行 改为  form，image
2、base.less中  fl fr     增加fl和fr
3、base.less中的@color     在其他的less中不能使用
4、base.less中的 * padding0   margin0  好像没有用
	，可能是让bootstrap中的所影响
	，在index.less中重新设置
5、页面的命名由首页中的  nav titlehref来决定 (同一个文件夹中)
6、productCity页面的传送  汉字不能进行页面之间传送
7、“更多”按钮动态生成后a标签的 href 会进行页面刷新
8、固定搜索栏:
	1、position设置后注意设置width 100%
	2、移动距离是顶部的高度，不是自身的高度
	3、下面元素的marginTop设置的是自身的marginTop值+固定定	   位元素的offsetHeight值
9、加载动画原理是利用动画和class  来让加载效果在加载前显示加载
   后去除;
	      
10、对一个option修改selected属性时  应使用.prop();

* */


问题：1.链接层级 问题  z-index 得配合定位使用
      2.缓加载，多次频繁发送请求:
	要使用节流阀 防止在scroll事件中持续监听 而造成多次重复请求。设置节流阀 防止频繁多次加载 。定义一个
	flag = true; 
	请求事件的时候变成false；complete 的时候变成true；
		
      3.缓加载理清逻辑不要造成多次重复渲染；

      4.ajax获取数据的时候  一定要清楚数据的内容是否包括结构，从而分析结构 再搭建静态结构和写CSS；

      5.响应式要注意 字体换行对结构的影响；设置最大宽度 最大高度 ，同时配合以下属性（超出隐藏 ，限制文本行数）
		-webkit-line-clamp：2；限制块级元素的文本行数

		display: -webkit-box;	弹性伸缩盒子

		-webkit-box-orient：vaertical	伸缩盒子内子盒子的排列方式     前三个属性必须配合使用

		text-overflow: ellipsis;多行文本超出用省略号表示 

		over-flower：hidden；

		只支持WebKit内核的的浏览器  在移动端 兼容性还行

      6.中间100%宽度  两边固定宽度布局结构；右边的盒子浮动一直有问题     
	解决：使用圣杯布局 ，绝对定位 ，都能解决

      7.	字体图标引用不熟：
		web字体图标的使用
        	 	第一 先在阿里下载 文件 并获取文字编码
		 	第二 引入文件（也可以不引入Css文件 但是要获取里面的编码 ，而且里面的引用文件的路径要与相应文件对应）
	 		第三 在类名中设置 字体的样式
         		第四 给使用web字体的标签（必须是空标签）加入类名
 	 		第五 标签里面写入字体编码；

     8.渲染页面的时候：偶尔出现页面数据错乱。 

		解决办法：
	利用each方法的回调函数中的参数  找到指定的li标签。

	（不同于例遍 jQuery 对象的 $().each() 方法，此方法可用于例遍任何对象。

	回调函数拥有两个参数：第一个为对象的成员或数组的索引，第二个为对应变量或内容。

	如果需要退出 each 循环可使回调函数返回 false，其它返回值将被忽略。）

	发现：click事件中绑定错误，用this绑定，this此时指的是13个li标签。
	给一个li标签绑定了13个渲染事件,利用each方法的回调函数中的参数找到指定的li标签。

     9.左右滑动   
	响应式中左右滑动的父盒子 宽度因为是自适应宽度。
	在第一次变换窗口滑动执行的时候，捕捉不到新的窗口宽度。
	容易出现子盒子定位不准，滑动错误；封装一个与onresize事件作用相同的窗口监听事件来解决；
*******************************************

* jQ方法忘了很多,记住的也很模糊,基本是现用现查,比如在写第一个页面的点击动画时为了判断元素是否为display:none耽误了很长时间;做页面布局时往往欠缺考虑,静态页面虽然写出来了,但是该动态渲染时才发现布局方式不合适.
* 逻辑混乱,写js时没有里顺思路,写的差不多了才发现问题,还经常脑袋一热修改前面写好的代码,过了一会儿出了问题又不知道是哪的原因,经常忘记备份,把代码改的论七八糟想回到原样时又回不去
* =========category页面=========
* ------布局问题-----
* 第一次使用table布局,将tr设置为display:flex, td设置为flex:1,每个tr里放3个td; 为table设置border-collapse无效,可能是与display:flex有冲突;

* 第二次使用.row > col-xs-4布局,每行放三个小格子,放不下就会自动换行,row自带左右-15px的margin,所以设置margin:0;设置row下面的每个小格子下边框和右边框,第nth-child(3n)个小格子无右边框,尽量不与row的爷爷盒子的边框重复
*

* ---js问题-----
* 第一次使用的是$.get方式,发送两次请求,后来使用$.ajax方式,因为要确保获取目录后再获取详细分类,所以把第二次请求放在第一次的complete里面执行

* 第一次写点击显示/隐藏分类时将运行放错了位置,导致在详细分类未渲染出前绑定事件,所以无效果,后来设置为等到页面内容全部渲染完成后再执行
* 写点击显示/隐藏分类时想实现动画效果,不知道怎么判断元素是否是display:none;后来用add和remove类名的方法实现点击显示/隐藏的效果,但是没有动画;最终在组长的指导下使用.css('display') == 'block')来判断元素是否为display:none,用slideDown和slideUp实现动画效果
*

*
* ======productList页面========
*---css----

*select里面的页码默认靠左显示,最终设置左padding将页码向右挤过去

* ------js-----

*,在改变页码时要使用empty()清空上次列表,显示加载动画;
*第一次写了两个请求,第一次请求生成的的是商品列表,第二次请求的是总商品数和每页显示的商品数,两者相除取得页数,后来发现得到的可能是小数,所以要向上取整,由此得到总页数;后来写了一个获得页数的函数,在第一请求的时候便获得页数,有几页就生成几个option放在select里,实现第一加载时只发送一次请求
* 

* 写点击翻页时先使用!(numb > 0 && numb <= max)来设置此区间点击无效,再判断点的是上一页还是下一页,若点击的是上一页,当前页为第一页时,return false,否则虽然不会刷新页面内容但是还是会发送请求,点击下一页同理
* 
 
*************************************************************

总体整合

	1、注意点：每个人引入的其他的js及css须引入项目中；

	2、注意点：没有search部分的页面，在common.js中不能执行，会报错；

	3、报错：header和footer部分rem布局。					解决：须在common.js中持续监听；

	4、样式错乱：header部分不使用公共样式时，common.less引用样式覆盖问题。	解决：整合进入单页less文件中。

	5、注意点：文件命名应严格使用主页API中的a标签href属性值进行设置。

	6、方法丢失问题：在common.js中设置的方法因为在闭包中，在下面不能引用。	解决：设置为$的方法。

	7、样式问题：如果绝对（固定）定位后，没有进行top 等的设置，位置不发生改变。
	
	8、压缩注意点：
		1、用gulp对less处理为css后，就不用再使用less.js进行解析；
		2、less用css替换之后，注意 stylesheet/less  要去掉/less，   而不是替换为stylesheet/css；
		3、压缩html之前先对内部html的引用测试之后再进行；（最后压缩html）



